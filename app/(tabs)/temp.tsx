import React from "react";
import { StyleSheet, Text, View } from "react-native";

const temp = () => {
  return (
    <View>
      <Text>temp</Text>
    </View>
  );
};

export default temp;

const styles = StyleSheet.create({});
